fix: 任务清单打勾后文字色与删除按钮丢失、恢复窗口可调整大小、勾选沉底排序

- TextBox/删除按钮文字色改为 XAML 绑定 List.TextColor，避免勾选刷新后变回黑色
- ResizeMode 改回 CanResize 且保留 ResizeBorderThickness，可拖拽边缘调整但不显示 grip 标识
- 改用 CollectionView 按 IsDone 排序实现勾选沉底，避免重建容器导致样式丢失
- AddTask/ApplyTextStyle 移除手动设置 Foreground 的重复逻辑